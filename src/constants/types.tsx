export type About = {
  name: string;
  location: string;
  desc: string;
};

export type Portfolio = {
  title: string;
  url: string;
  repo: string;
  desc: string;
  dependencies: string[];
  time: string;
};

export type Contact = {
  name: string;
  url: string;
};
